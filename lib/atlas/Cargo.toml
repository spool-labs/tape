[package]
name = "tape-atlas"
version.workspace = true
edition = "2021"
description = "Tapedrive Atlas: shared wire types for the live network globe (stats, topology, traffic events)."
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
bincode = "1"
